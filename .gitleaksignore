# Fingerprints for gitleaks findings verified as non-secret, not stripped from
# history because that would rewrite published commits for no security gain.
# Each is `<commit>:<file>:<rule>:<line>`, generated by running gitleaks
# itself against this repo — never hand-typed. Confirmed at
# https://github.com/Vantaneant-International-Ltd/tacet/pull/1

# .dev.vars.example:32 — a commented-out setup instruction with a literal
# "..." placeholder ("AP_PRIVATE_KEY=\"-----BEGIN PRIVATE KEY-----\n...\n...\""),
# never a real key.
633016fe0806ac9ebf522f4a8c9bc84d77e0c824:.dev.vars.example:private-key:32

# test/openweb.test.ts (line 190 in the flagged commit; 340 on current main,
# git history line numbers reflect the commit, not HEAD) — a template literal
# that builds a fake PEM string at runtime from a random test keypair's base64
# encoding, to test PEM-wrapping. No key is embedded in source.
633016fe0806ac9ebf522f4a8c9bc84d77e0c824:test/openweb.test.ts:private-key:190
