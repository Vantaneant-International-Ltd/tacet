# Handoff — Landing (canonical)

> **Status: CANONICAL.** Delivered directly by the operator (chat, 2026-07-09) as the
> authoritative visual target for the public landing at `/`. Supersedes the earlier
> dark-only landing. The non-authoritative Today sketch under
> [`../prototypes/today-desktop-dark.html`](../prototypes/today-desktop-dark.html) is a
> different surface and remains a sketch, not handoff.
>
> Repo law still wins on conflict: [`../../tokens.md`](../../tokens.md),
> [`../../design-principles.md`](../../design-principles.md), the doctrine refusals, and
> ADR-011 / ADR-012. Nothing below reintroduces a refused pattern.

The exports arrived as three rendered screens rather than files. Captured here so the
target survives the session.

## Structure (top → bottom)

1. **Top nav** — placeholder sunburst mark + `tacet` wordmark (left); `Sign in` link +
   `Join the beta` gradient pill (right). Sits over the dark hero.
2. **Hero (dark)** — moving constellation background (warm + cool nodes on an ellipse).
   Headline **"The social web."** (ink) + **"Finally."** (accent purple). Lead:
   *One identity. Your people. No walls.* Primary `Continue →` (light pill on desktop);
   `Already have an account? Sign in`. Scroll cue at the base.
3. **Fragmentation (light)** — left: *Today's internet is fragmented.* /
   *Six identities. Six inboxes. Six algorithms. Six companies.* Right: a row of platform
   cards (Instagram, TikTok, Reddit, X, LinkedIn, Mastodon, Pixelfed, PeerTube, And more)
   with lines converging downward.
4. **Convergence (light lavender)** — left: *Tacet brings it all together.* /
   *One identity. One inbox. One place. The open social web.* Right: orbital diagram, the
   Tacet mark at the centre with people + platforms around it.
5. **Divided (light)** — *The internet was never meant to be this divided.* with a
   many-nodes → one-node convergence diagram; aside: *The open social web was always the
   future.* (This replaces the old "work like email" federation panel, which used protocol
   jargon and raw `@user@instance` handles — refused, now removed.)
6. **Final CTA (dark purple)** — *Be part of something better.* /
   *Join the beta and help shape a new kind of social experience. Built for people, not
   platforms.* `Join the beta →` gradient pill; `Already have an account? Sign in`.
7. **Footer** — mark + Manifesto · Privacy · Terms · Status · GitHub · Contact ·
   `© 2026 Tacet. All rights reserved.`

## Notes

- Logo is a **placeholder** sunburst mark by operator instruction; swap when a real mark
  lands.
- Onboarding "Where do you live online?" is a **separate** surface (a signup step), not the
  landing — the platform picker does not live inline on `/`.
- Refusals honoured: no counts on own posts, no streaks/badges, no infinite scroll, no
  autoplay, no protocol language in copy, no manufactured urgency. "Join the beta" is an
  invitation, not urgency.
