The only way to render product icons — Lucide set, outline, 2px stroke, currentColor.

```jsx
<Icon name="house" />
<Icon name="bell" size={20} style={{ color: 'var(--text-secondary)' }} />
```

Keep strokeWidth at 2. Nav map: today→sun, people→users, discover→compass, communities→users-round, compose→square-pen, federation→network.
