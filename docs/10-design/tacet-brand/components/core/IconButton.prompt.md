Square icon-only button for toolbars, card actions, and nav chrome.

```jsx
<IconButton name="bell" label="Notifications" />
<IconButton name="bookmark" active />
```

`active` switches to accent text colour. Always pass `label` for accessibility.
