Desktop sidebar navigation. Active item gets a soft accent pill.

```jsx
<SideNav value={view} onChange={setView} footer={<PersonRow/>} />
```

Default items are the six Tacet destinations; pass `items` to customise.
